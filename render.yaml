# Render Blueprint — Docker image (root Dockerfile).
# New → Blueprint → connect repo. Free tier may spin down when idle.

services:
  - type: web
    name: ai-farm-water
    runtime: docker
    region: oregon
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: FLASK_ENV
        value: production
